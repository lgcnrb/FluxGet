<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FluxGet.UI.Views.YouTubeDownloadDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Spacing="16" Padding="24">
        
        <!-- Loading -->
        <StackPanel x:Name="LoadingPanel" VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="12">
            <ProgressRing x:Name="LoadingRing" IsActive="True" Width="48" Height="48"/>
            <TextBlock x:Name="LoadingText" Text="YouTube video bilgisi aliniyor..."
                       HorizontalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        </StackPanel>
        
        <!-- Video Info -->
        <StackPanel x:Name="VideoInfoPanel" Visibility="Collapsed" Spacing="12">
            
            <!-- Baslik -->
            <TextBlock x:Name="TitleText" FontSize="18" FontWeight="SemiBold" TextWrapping="Wrap"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="UploaderText" FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,16,0"/>
                <TextBlock Grid.Column="1" x:Name="DurationText" FontSize="12"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </Grid>
            
            <!-- Format Secimi -->
            <TextBlock Text="Format Sec" FontSize="14" FontWeight="SemiBold" Margin="0,8,0,0"/>
            
            <RadioButtons x:Name="FormatRadioButtons" SelectionChanged="FormatRadioButtons_SelectionChanged">
                <!-- MP4 secenekleri -->
                <RadioButtons.Header>
                    <StackPanel Orientation="Horizontal" Spacing="4">
                        <FontIcon Glyph="&#xE714;" FontSize="14" VerticalAlignment="Center"/>
                        <TextBlock Text="Video (MP4)" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </RadioButtons.Header>
            </RadioButtons>
            
            <!-- Kaydet yolu -->
            <StackPanel Spacing="4" Margin="0,8,0,0">
                <TextBlock Text="Kaydet Konumu" FontSize="13" FontWeight="SemiBold"/>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" x:Name="PathText" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis"
                               FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               VerticalAlignment="Center"/>
                    <Button Grid.Column="1" Content="Degistir" Click="ChangePathButton_Click" Margin="8,0,0,0"/>
                </Grid>
            </StackPanel>
            
            <!-- Hata mesaji -->
            <TextBlock x:Name="ErrorText" Text="" FontSize="12" Foreground="Red" 
                       TextWrapping="Wrap" Visibility="Collapsed"/>
            
        </StackPanel>
    </StackPanel>
</Page>
