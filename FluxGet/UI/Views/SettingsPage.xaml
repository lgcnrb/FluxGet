<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FluxGet.UI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer Padding="32,20,32,20" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="16">
            <StackPanel.ChildrenTransitions>
                <TransitionCollection>
                    <EntranceThemeTransition/>
                </TransitionCollection>
            </StackPanel.ChildrenTransitions>

            <!-- Header -->
            <StackPanel Orientation="Horizontal" Spacing="10" Margin="0,0,0,8">
                <FontIcon Glyph="&#xE713;" FontSize="28" VerticalAlignment="Center"/>
                <StackPanel Spacing="4">
                    <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold"/>
                    <TextBlock Text="Configure application preferences"
                               FontSize="12"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </StackPanel>

            <!-- Genel Ayarlar -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                    <FontIcon Glyph="&#xE713;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="General" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Default Download Location" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Folder where downloaded files will be saved" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <Button Grid.Column="1" Content="Browse..."
                                    Margin="12,0,0,0" Click="BrowseButton_Click"/>
                        </Grid>
                        <TextBlock x:Name="PathText" Text="{Binding DefaultDownloadPath}"
                                   FontSize="12" Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                   TextTrimming="CharacterEllipsis" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Auto Start Downloads" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Automatically start new downloads" FontSize="11"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{Binding IsAutoStartDownloads, Mode=TwoWay}"
                                      OnContent="On" OffContent="Off" VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Notifications" FontWeight="SemiBold" FontSize="13"/>
                            <TextBlock Text="Show notification when download completes" FontSize="11"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                        <ToggleSwitch Grid.Column="1" IsOn="{Binding IsShowNotifications, Mode=TwoWay}"
                                      OnContent="On" OffContent="Off" VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- Downloads Settings -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,8">
                    <FontIcon Glyph="&#xE896;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Downloads" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Max Concurrent Downloads" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Number of concurrent downloads" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" x:Name="ConcurrentText"
                                       Text="{Binding MaxConcurrentDownloads}" FontSize="16" FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <Slider x:Name="ConcurrentSlider" Minimum="1" Maximum="20"
                                Value="{Binding MaxConcurrentDownloads, Mode=TwoWay}"
                                StepFrequency="1" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Global Speed Limit" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="General speed limit for all downloads (KB/s)" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <TextBlock Grid.Column="1" x:Name="SpeedLimitText" Text="Unlimited"
                                       FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </Grid>
                        <Slider x:Name="SpeedLimitSlider" Minimum="0" Maximum="10240"
                                Value="{Binding GlobalSpeedLimit, Mode=TwoWay}"
                                StepFrequency="100" ValueChanged="SpeedLimitSlider_ValueChanged"
                                Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Tarayici Eklentisi -->
            <StackPanel Spacing="4">
                <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,8">
                    <FontIcon Glyph="&#xE774;" FontSize="16" VerticalAlignment="Center"/>
                    <TextBlock Text="Browser Extension" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <Border Padding="16" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                        CornerRadius="10" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Enable Browser Extension" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Text="Direct download from browser support" FontSize="11"
                                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,2,0,0"/>
                            </StackPanel>
                            <ToggleSwitch Grid.Column="1" IsOn="{Binding IsBrowserExtensionEnabled, Mode=TwoWay}"
                                          OnContent="On" OffContent="Off" VerticalAlignment="Center"/>
                        </Grid>
                        <TextBlock x:Name="ExtensionStatusText"
                                   Text="{Binding ExtensionStatus, FallbackValue='Status: Unknown'}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   Margin="0,8,0,0" FontSize="12"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Aksiyonlar -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                    CornerRadius="10" Padding="16" Margin="0,8,0,0">
                <Grid>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                        <Button Content="Reset Defaults" Click="ResetDefaultsButton_Click"/>
                        <Button Content="Save" Style="{StaticResource AccentButtonStyle}" Click="SaveButton_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
